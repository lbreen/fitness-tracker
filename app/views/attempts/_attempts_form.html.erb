<%= simple_form_for([@exercise, @attempt], remote: true) do |f| %>
  <%= f.error_notification %>

  <%= f.input :date %>
  <%= f.input :time_taken %>
  <%= f.input :comments %>
  <%= f.button :submit, class: "btn btn-primary" %>

<% end %>
